{
  "name": "which-key",
  "main": "./lib/which-key",
  "version": "0.0.0",
  "description": "Display available keybindings in a panel",
  "keywords": [],
  "repository": "https://github.com/atom/which-key",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "etch": "^0.8.0"
  },
  "configSchema": {
    "failedTimeout": {
      "type": "number",
      "default": 1,
      "minimum": 0,
      "maximum": 10,
      "description": "How long the panel showing a failed keystroke will be shown"
    },
    "isVisible": {
      "type": "boolean",
      "default": true,
      "description": "Show the key bindings guide at the bottom of the workspace"
    },
    "keystrokesFormat": {
      "type": "string",
      "default": "short",
      "enum": [
        { "value": "long", "description": "Atom style: CTRL-w, ALT-b, BACKSPACE, RIGHT" },
        { "value": "short", "description": "Emacs style: C-w, M-b, BACKSPACE, RIGHT" },
        { "value": "symbol", "description": "Keys symbols: \u2303-w, \u2325-b, \u232B, \u2192." }
      ],
      "description": "Format for the keystrokes regarding individual and modifier keys"
    },
    "useMetaForAlt": {
      "type": "boolean",
      "default": true,
      "description": "Show the `alt` key as `meta`"
    }
  }
}
